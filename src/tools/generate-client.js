// const { exec } = require('child_process');
// const path = require('path');

// const backendProject = path.resolve(__dirname, '../../rajmango_core/WebApi');

// console.log('📦 Generating NSwag TypeScript client...');
// exec(`dotnet run --project "${backendProject}"`, (error, stdout, stderr) => {
//   if (error) {
//     console.error(`❌ Error:\n${error.message}`);
//     return;
//   }
//   if (stderr) {
//     console.error(`⚠️ Warnings:\n${stderr}`);
//   }
//   console.log(`✅ Done:\n${stdout}`);
// });
